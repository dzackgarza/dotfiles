% DZG Core Package Loading
% Shared preamble fragment included by dzg-unified.sty and dzg-tikz.sty.
% Package loading only — tier macro inclusions are each .sty's own concern.

% Core math packages (required for macros to work)
\RequirePackage{amsmath,amsthm,amssymb,amsfonts}
\RequirePackage{mathtools}    % DeclarePairedDelimiter, coloneqq
\RequirePackage{mathrsfs}     % Script fonts
\RequirePackage{bm}           % Bold math
\RequirePackage{esint}        % Integral symbols
\RequirePackage{stmaryrd}     % Special brackets
\RequirePackage{upgreek}      % Upright Greek

% Graphics (conditional - may be loaded by document with options)
\@ifpackageloaded{graphicx}{}{\RequirePackage{graphicx}}
\@ifpackageloaded{xcolor}{}{\RequirePackage[dvipsnames]{xcolor}}

% TikZ and diagrams
\RequirePackage{tikz}
\RequirePackage{tikz-cd}
\usetikzlibrary{arrows.meta, cd, fadings, patterns, calc, matrix, positioning, decorations, decorations.pathreplacing, decorations.markings, shapes, backgrounds, fit, shapes.geometric, intersections, hobby, matrix,arrows,decorations.pathmorphing}
\tikzfading[name=fade out, inner color=transparent!0, outer color=transparent!100]

% Utilities (if not already loaded)
\@ifpackageloaded{etoolbox}{}{\RequirePackage{etoolbox}}
\@ifpackageloaded{xparse}{}{\RequirePackage{xparse}}

% Diagrams and Plots
\RequirePackage{tikzit}
\RequirePackage{dynkin-diagrams}
\RequirePackage{quiver}
\RequirePackage{pgfplots}
\RequirePackage[all]{xy}

% Graphics path for \includegraphics in diagrams
\graphicspath{{~/figures/}}

% Microtype: protrusion and expansion
\RequirePackage[activate={true,nocompatibility}, final, tracking=true, kerning=true, spacing=true]{microtype}

% Hyperlinks
\RequirePackage[colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue]{hyperref}

% List spacing
\RequirePackage{enumitem}
