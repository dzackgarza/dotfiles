
. "$HOME/.cargo/env"

# NVM default selected by ~/.zprofile; cheap inheritance for every zsh.
export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"
if [[ -x "$NVM_DIR/current/bin/node" ]]; then
    export PATH="$NVM_DIR/current/bin:$PATH"
fi


# Direnv
eval "$(direnv hook zsh)"

